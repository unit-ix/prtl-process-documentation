# Datenmodell

> **Snapshot — nicht die Live-Wahrheit.** Stand: `<YYYY-MM-DD>` · Quelle: SharePoint `Projekte/<Kunde> - <Projekt>/Konzepte/` (lokal via OneDrive-Sync) · Aktualisieren: `/prototype`-Ingest erneut (`[O]verwrite`).
> SharePoint ist die laufend gepflegte Single Source of Truth; diese Kopie wird beim Ingest gespiegelt und kann veralten.

Beschreibung der Dataverse-Tabellen und ihrer Beziehungen für dieses Projekt.

Das Mermaid-ERD liegt in [`datamodel.mmd`](datamodel.mmd) — direkt rendernd in GitHub, VSCode und Obsidian.

## Tabellen

> Vor Dev-Start befüllen. Jede Tabelle kurz beschreiben: Zweck, wichtige Spalten, Beziehungen.
